name: Tags_CI
on:
  push:
    tags:
      - "*"
jobs:
  check-bats-version:
    runs-on: self-hosted
    steps:
      - name: checkout
      - uses: actions/checkout@v4
      - run: ls
      - run: tail index.js
